{
  "version": "0.0.1",
  "configurations": [
    {
      "//": "vite invoked directly rather than through `pnpm exec`. Going through pnpm makes starting the dev server depend on which pnpm happens to be on PATH: this project's node_modules is installed by pnpm 10 (the major every workflow pins with `pnpm/action-setup`), and a global pnpm 11 refuses to run any script until it has purged and reinstalled that directory \u2014 `ERR_PNPM_ABORTED_REMOVE_MODULES_DIR_NO_TTY`, because the purge wants a TTY to confirm. The two fixes pnpm suggests, `CI=true` and `confirmModulesPurge=false`, both *authorise* the purge, which would delete node_modules underneath whatever else is using it. The dev server needs none of that: the binary is already on disk.",
      "name": "builderhunt-dev",
      "runtimeExecutable": "./node_modules/.bin/vite",
      "runtimeArgs": [
        "dev"
      ],
      "port": 3010,
      "autoPort": false
    },
    {
      "//": "A second port so a parallel session can run its own dev server. Port 3010 belongs to whichever session started first; this one takes 3011 rather than failing, and vite is invoked directly for the same pnpm-major reason recorded on the entry above.",
      "name": "builderhunt-dev-3011",
      "runtimeExecutable": "./node_modules/.bin/vite",
      "runtimeArgs": [
        "dev",
        "--port",
        "3011"
      ],
      "port": 3011,
      "autoPort": false
    }
  ]
}
