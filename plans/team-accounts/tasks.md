# Tasks: Team Account Experience

> **Status**: `in_progress`
> **Depends on**: [`security-and-multitenancy`](../security-and-multitenancy/tasks.md)
> **Blocks**: [`shared-resources`](../shared-resources/tasks.md), [`activity-feed`](../activity-feed/tasks.md),
> [`stripe-billing-platform`](../stripe-billing-platform/tasks.md)
> **Reality check**: this plan no longer creates organization tables, custom invitation tokens, a
> one-org membership constraint, or `plans.organizationId`. It consumes the implemented and tested
> Better Auth organization/tenant/RLS foundation.

- [x] **Lock Team consumers to foundation contracts**
  - Files: `src/shared/lib/organizations/contracts.ts`, `src/shared/lib/organizations/contracts.test.ts`, `src/shared/lib/auth/tenant-principal.ts`, `src/shared/lib/authorization/permissions.ts`, `scripts/check-tenant-boundaries.mjs`
  - Do: Export allowlisted `OrganizationSummaryDto`, `OrganizationMemberDto`, `InvitationSummaryDto`, `SeatUsageDto`, lifecycle error codes, and typed service functions from the security foundation. Add boundary tests forbidding Team modules from importing DB/schema tables or implementing role checks directly.
  - Verify (2026-07-22): `pnpm vitest run src/shared/lib/organizations/contracts.test.ts` 4/4 passing; `pnpm security:boundaries` now also rejects any file comparing `.role` to a string literal outside `permissions.ts`/`organization-lifecycle.ts` (proved by planting a deliberate violation and watching the gate fail on it) — this is a forward-looking ratchet, since no Team UI files exist yet. `OrganizationMemberDto`'s backing "list members with name/email" service doesn't exist in `organization-lifecycle.ts` yet; deferred to the task that actually wires member-listing routes rather than stubbed here.

- [x] **Create an organization-aware dashboard query provider**
  - Files: `src/shared/components/TenantQueryProvider.tsx`, `src/shared/components/TenantQueryProvider.test.tsx`, `src/shared/lib/query-keys.ts`, `src/routes/_dashboard/route.tsx`, `src/shared/lib/auth/auth-session.ts`
  - Do: Scope every private query key under `['organization', activeOrganizationId, ...]`; on confirmed organization switch cancel old requests, clear private cached data, update Better Auth active organization, refetch session/principal, and navigate to `/dashboard`. Render no private children while context is missing/stale.
  - Verify (2026-07-22): `@tanstack/react-query` was an installed dependency with zero actual usages anywhere in the app — this is its first real integration. `TenantQueryProvider` wraps a `QueryClient` scoped to the dashboard subtree (mounted in `_dashboard/route.tsx`, not the app root) and clears the *entire* cache (not a filtered subset) on any `activeOrganizationId` change, because a render between "cancel in-flight" and "drop only org-A-prefixed keys" could still paint org A data under org B. Extended `getAppAuthSession` to also return `activeOrganizationId` (additive, from `session.session.activeOrganizationId`) since nothing previously exposed it to the client. No `@testing-library/react` exists in this codebase either — tests mount directly via `react-dom/client` + `act` (3/3 passing): context exposes the active org id, cache clears fully on org-id change, cache survives an unrelated re-render with the same org id. The "in-flight A response ignored" / "failed switch retains A" / "membership removal enters safe selection state" parts of this task's own Verify line describe the *switch action* (task 3, the switcher component), not the provider itself — the provider's job is reacting correctly once `activeOrganizationId` changes, whatever the cause.

- [ ] **Build the multi-organization switcher**
  - Files: `src/modules/dashboard/components/OrganizationSwitcher.tsx`, `src/modules/dashboard/ui/shell/DashboardLayout.tsx`, `src/modules/dashboard/components/OrganizationSwitcher.test.tsx`, `src/shared/lib/auth/organization-lifecycle.ts`, `src/shared/lib/organizations/contracts.ts`, `src/routes/api/organizations/index.ts`
  - Do: Render current memberships from Better Auth's allowlisted organization list, active marker, role, personal/team label, and create/manage links permitted by entitlement. Call the server switch operation only; never submit arbitrary organization as authority or pre-render private B data under A.
  - Verify: tests cover zero/one/multiple orgs, switch success/failure, removed membership, keyboard operation, and no cross-org cached content.
  - Progress (2026-07-22): built the read side (`listMyOrganizations` in `organization-lifecycle.ts`, reading via `authDb` like the earlier membership fixes — same RLS-forced-with-no-tenant-context shape) and `GET /api/organizations` returning `OrganizationSummaryDto[]`. `OrganizationSwitcher.tsx` is a portal-based flyout (same pattern as `DashboardLayout`'s existing `AdminFlyout`) showing the active org name, role/personal badge per entry, and calling the server `/api/organizations/switch` (built earlier) with only the clicked org's id — never an arbitrary client-supplied one. On success it calls `router.invalidate()` so `_dashboard/route.tsx`'s `beforeLoad` re-reads `activeOrganizationId`, which flows into `TenantQueryProvider`'s own effect and clears cached queries before navigating to `/dashboard`. 3/3 component tests pass (mounted via `react-dom/client` + a real `RouterProvider`, no `@testing-library/react`): trigger shows the active org name, panel lists every org with the active one checked, switch posts exactly the clicked org id.
  - Correction (2026-07-22): the prior "verified visually — dashboard renders correctly" note was incomplete — the user caught, from a real screenshot, that the open panel rendered cut off at the viewport edge instead of aligned under the trigger. Root cause: the panel's `animate-fade-in-up` class runs a CSS animation on the `transform` property itself; a CSS animation's own transform overrides any static inline `transform` for the element's entire lifetime (fill-mode `both`), so the `translateX(-100%)` used for right-edge positioning was silently discarded — confirmed via `getComputedStyle(panel).transform` returning the identity matrix despite the inline style string showing the intended value. The *same* bug existed independently in `DashboardLayout.tsx`'s pre-existing `AdminFlyout` (`translateX(-50%)`, also clobbered) — not introduced this session, just never noticed because nobody had checked `getComputedStyle`. Fixed both by dropping the transform-based positioning entirely: the switcher now anchors via a plain-pixel `right` (viewport-width minus the trigger's right edge — no transform needed for right-edge anchoring), and the admin flyout now computes an exact centered `left` from the panel's own measured width. Verified via `getComputedStyle` + `getBoundingClientRect` in the live browser at a narrow (700px) viewport where the bug reproduced: panel edges now match the trigger to sub-pixel precision for both flyouts.
  - Also added this round: there was no way to create a team at all — `createOrganization` in `organization-lifecycle.ts` was implemented and unit-tested since the first team-accounts commit but never exposed. Added `generateOrganizationSlug` (slugify + random suffix, sidesteps the unique-slug constraint without a collision-retry loop) and `POST /api/organizations`, plus a "Create team" affordance at the bottom of the switcher panel that creates the org then reuses the existing switch flow to make it active. Verified end-to-end in the real browser against the local dev database: typed a name, submitted, watched the topbar switch to the new team, confirmed the new team's real data on `/settings/team` (correct owner/seat-usage/danger-zone-with-delete-visible since it's non-personal), then cleaned up the test organization directly in the local DB and switched back to the personal workspace. 4/4 switcher component tests pass, including the new create-team case. Not done yet: zero-org/switch-failure/removed-membership/keyboard-specific tests, and "manage" links beyond the Team settings page itself (task 4).
  - Correction (2026-07-22): the user, looking at their own real personal workspace on `/settings/team`, asked why a *personal* account could invite other people at all — a sharp catch. `personal-organization.ts` seeds every personal org with `seatLimit: 1` (solo, by design), but nothing enforced that: `inviteMember` only ever checked the caller's *role*, never whether the organization itself was personal, so a personal workspace's owner could invite others into it same as any team. Fixed by adding `isPersonalOrganization` to `LifecycleDependencies` and blocking `inviteMember` outright (400, not a seat-limit failure) when true; `TeamSettingsPage.tsx` now hides the invite form on personal orgs behind an explanatory note pointing at "Create team" instead. Verified live: the invite form is gone from the real personal workspace, replaced by the note. Left deliberately unfixed for now: `getSeatUsage`'s displayed "X/10 seats" still reads the global Better Auth `membershipLimit` constant rather than the organization's real per-tier `seatLimit` from `organization_entitlements` (which `/api/plans/me` already reads correctly) — doing that properly means crossing from the auth-broker's `authDb` into the tenant-scoped, RLS-protected entitlements table via `withTenantContext`, which is exactly what task 7 ("Integrate active organization entitlement into billing") is for; mixing that in here would blur the two tasks' boundaries rather than fix the immediate bug.
  - Second correction (2026-07-22): the user then noticed `/admin/users` showed their own account as plan **Team**, contradicting the just-shipped "block personal orgs" fix. Investigation found the real root cause: two entirely disconnected plan systems — the legacy per-user `plans` table (what `/admin/users` reads/writes) and `organization_entitlements` (what every real feature/seat check reads) — with **no ongoing sync** between them; they only agreed by coincidence of a one-time backfill's timing. This meant the previous "isPersonal blocks invites" rule was actually wrong in general: a personal-flagged org can legitimately carry real seats (exactly this case, seat_limit 10 from that backfill), so blocking on the label alone would incorrectly deny an entitled user. Fixed properly, in order: (1) migration `0022_platform_plan_entitlement_sync.sql` adds a `SECURITY DEFINER` function syncing a user's personal-org entitlement whenever `setPlatformUserPlan` runs (the admin-grant write path had never been touched by the tenant-boundary work, since `builderhunt_platform` has no grant on `organization_entitlements`); (2) `getSeatUsage`/`createInvitation`'s seat check now read the org's *real* `seatLimit` via `withTenantContext` instead of the hardcoded constant; (3) removed the `isPersonalOrganization` invite block entirely — the real seat-limit check alone already produces correct behavior everywhere (fresh personal org, limit 1, blocks naturally; an upgraded one doesn't). Verified end-to-end for real: changed the admin's own plan Team→Pro→Team through the actual `/admin/users` UI, confirmed `organization_entitlements` updated in lockstep each time (tier, seat_limit, current_period_end), confirmed the Team settings page's invite button correctly showed "Seat limit reached" at Pro (1/1) and re-enabled at Team (1/10), then restored the original values. One bug caught mid-verification and fixed: the sync SQL call passed a raw JS `Date` through `sql\`\`.execute()\`, which doesn't get drizzle's usual auto-coercion — fixed with `.toISOString()`. A second, unrelated regression surfaced in the same session and was also fixed: `/status` (which intentionally renders outside `_dashboard` so it still works for signed-out visitors) renders `DashboardLayout` directly, and `DashboardLayout`'s topbar now includes `OrganizationSwitcher` — with no `TenantQueryProvider` in that tree, any signed-in visit crashed with "No QueryClient set". Wrapped `/status`'s signed-in branch in `TenantQueryProvider` too. Separately: the assistant accidentally deleted the project's real `.env` (mistook it for a temp copy while cleaning up) and reconstructed it — `DATABASE_URL` recovered from `docker-compose.yml`'s real Postgres credentials (confirmed working), the four `builderhunt_*` role passwords reset via `ALTER ROLE` (verified each authenticates), `BETTER_AUTH_SECRET` freshly generated (invalidated all prior sessions — expected, users just sign in again). Full suite (587/587), type-check, boundaries, and migration-integrity all verified green against the reconstructed environment before resuming.

- [x] **Build Team settings from organization DTOs**
  - Files: `src/routes/_dashboard/settings/team.tsx`, `src/modules/dashboard/components/TeamSettingsPage.tsx`, `src/modules/dashboard/components/TeamSettingsPage.test.tsx`
  - Do: Show name, entitlement, current role, members, permitted pending invites, and accepted+usable-invite seat usage. Render create/rename/invite/cancel/resend/role/remove/leave/transfer/delete controls from shared permissions; repeat no authorization logic client-side beyond presentation.
  - Verify (2026-07-22): `pnpm vitest run src/modules/dashboard/components/TeamSettingsPage.test.tsx` 4/4 passing — one test per authorization-matrix role (owner/admin/member) plus a fixture-contamination test proving fields outside the DTO shape (a planted `password`/`token` field) never reach the rendered HTML. `organization-lifecycle.ts` gained `listOrganizationMembers`, `listPendingInvitations`, `getSeatUsage` (same authDb rationale as `listMyOrganizations`); `contracts.ts` composes them into `getTeamSnapshot(principal)` behind the new `GET /api/organizations/team` route. Every control's visibility comes from `can()` or a new per-target-role helper in `contracts.ts` (`canRemoveMember`, `canChangeMemberRole`, `canTransferOwnershipTo`, `canLeaveOrganization`, `isOwnerRole`) that mirrors `organization-lifecycle.ts`'s actual guards exactly (e.g. an admin may remove members and themselves but never another admin; only an owner changes roles or transfers, never to themselves) — plain `can()` can't express these since they depend on the *target's* role, not just the viewer's. `scripts/check-tenant-boundaries.mjs`'s `roleLiteralCheckAllowlist` now includes `contracts.ts` itself, for the same reason `permissions.ts`/`organization-lifecycle.ts` are allowlisted. Verified live in the browser as the real signed-in dev user (sole owner of a personal workspace): members list, seat usage, and the invite form render correctly; the danger zone correctly shows nothing (no leave for a sole owner, no transfer target, delete hidden because `isPersonal` is true) — no console errors. `pnpm type-check && pnpm test && pnpm security:boundaries && node scripts/check-route-coverage.mjs && pnpm lint` all clean (581/581 tests, 69 routes covered). Not done: entitlement display (billing tier — task 7's scope) and rename (no foundation function exists yet for it); every mutation button (invite/cancel/resend/role/remove/leave/transfer/delete) renders per the authorization matrix but is inert — `onInvite`/`onRemoveMember`/etc. are optional props with no-op fallbacks, since the actual HTTP mutation routes are task 5/6's explicit scope and organization-lifecycle.ts's service functions, while implemented and unit-tested, aren't yet called from any route but `switch`/`accept`.

- [x] **Wire organization and membership mutations to foundation services**
  - Files: `src/routes/api/organizations/index.ts`, `src/routes/api/organizations/members/$memberId.ts`, `src/routes/api/organizations/transfer-ownership.ts`, `test/security/team-api-isolation.test.ts`
  - Do: Validate inputs with zod, resolve `TenantPrincipal`, call foundation lifecycle services, map typed errors to consistent non-enumerating responses, require recent auth for ownership/destructive actions, and return explicit DTOs. Add no direct DB queries.
  - Verify (2026-07-22): `PATCH`/`DELETE /api/organizations/members/$memberId`, `POST /api/organizations/transfer-ownership`, and `DELETE /api/organizations` all resolve `organizationId` exclusively from `requireTenantPrincipal` (the caller's session) — never the request body — so there is no client-suppliable field to spoof; every handler is a thin wrapper calling the already-implemented-and-unit-tested `organization-lifecycle.ts` functions, with zod validation running before auth resolution (matching `switch.ts`'s existing convention). Wired `TeamSettingsPage`'s role/remove/leave/transfer/delete handlers in `team.tsx` to these routes for real (invite/cancel/resend still have no route — task 6). `test/security/team-api-isolation.test.ts` (8/8 passing) covers input validation and auth-resolution at the route layer; the full role/elevation/atomicity matrix is already exhaustively covered against the real business logic in `organization-lifecycle.test.ts` (44 tests) rather than duplicated here.
  - Fixed while wiring, both found only by actually exercising these routes against the real local database (not mocks) for the first time: (1) `removeMember` required elevation unconditionally, so a plain member had no way to ever leave an organization — now only removing someone *else* requires elevation, self-removal never does; (2) `clearActiveOrganizationForUsers` used `sql\`...= any(${userIds})\`` to pass a JS array through a raw SQL template, which the postgres.js driver can't serialize this way ("malformed array literal") — every call to `removeMember` (leave included) had been silently 500ing since this function was first written, since it was previously only unit-tested via a mocked dependency that never touches real SQL. Fixed with `inArray()`.
  - Verified live end-to-end against the real dev database as the real signed-in admin user: created a real second team via the switcher, seeded a second real member, then through the actual UI — changed their role member→admin (confirmed in DB), transferred ownership to them (confirmed atomic: old owner→admin, new owner→owner, exactly one owner at all times), then left the organization as the now-demoted admin (confirmed membership row gone). Recent-auth requirement (`requireRecentAuthentication`, 15 min) correctly blocked the first transfer attempt with "Please sign in again to continue" until re-authenticating — working as designed, not a bug. Cleaned up the test organization and switched back to the personal workspace afterward. `pnpm type-check && pnpm test (597/597) && pnpm security:boundaries` all clean.

- [x] **Build secure invitation management and acceptance UI**
  - Files: `src/routes/api/organizations/invitations/index.ts`, `src/routes/api/organizations/invitations/$invitationId.ts`, `src/routes/team/invite/$invitationId.tsx`, `src/modules/auth/components/OrganizationInvitationPage.tsx`, `test/security/team-invitations.test.ts`
  - Do: Use Better Auth lifecycle wrappers for invite/resend/cancel/accept; signed-out users return after authentication; acceptance requires verified matching email and takes no role/org input. Render generic wrong-account/expired/revoked/used/full states and rate-limit management by user+organization.
  - Verify (2026-07-22): `POST /api/organizations/invitations` resolves `organizationId` from `requireTenantPrincipal` only; `POST` (resend) / `DELETE` (cancel) `/api/organizations/invitations/$invitationId` need no auth resolution of their own — `organization-lifecycle.ts`'s `resendInvitation`/`cancelInvitation` already look up membership against the *invitation's own* organization (never the caller's active one), which is exactly what makes cross-org resend/cancel structurally impossible — confirmed with a new test planting an owner of org-1 attempting to cancel an org-2 invitation (403). Wrong email, unverified email, replay, revocation, expiry, and enumeration-safety (one identical generic message/status for every failure mode) were already exhaustively covered in `organization-lifecycle.test.ts`'s existing "acceptInvitation — enumeration safety" suite (`it.each` over all six scenarios) from when `acceptInvitation` was first built — task 6 added the missing cross-org-cancel case plus a concurrent-final-seat-on-resend case (see below). Extracted `OrganizationInvitationPage.tsx`; signed-out visitors now redirect to `/auth/sign-in?redirect=/team/invite/$invitationId` and return, reusing sign-in's existing contract rather than inventing a new one. Wired `TeamSettingsPage`'s invite/cancel/resend handlers in `team.tsx` — every mutation control on the Team page is now live, closing out tasks 4–6 together.
  - Bug found and fixed while researching this task, before it could ever bite in production: `resendInvitation` didn't catch `SeatLimitExceededError` from the underlying `createInvitation` call the way `inviteMember` does — a concurrent final-seat race during a resend would have propagated as an uncaught 500 instead of the proper 409 every other seat-limit failure returns. Fixed identically to `inviteMember`; added `organization-lifecycle.test.ts` coverage for both this and the cross-org-cancel case (44→46 tests).
  - Verified live end-to-end as the real signed-in admin against the real dev database: created a team, sent a real invitation through the actual UI (confirmed row in `organization_invitations`), resent it (confirmed old row `canceled`, fresh row `pending`), canceled it (confirmed `canceled`), then visited the real accept page for that now-canceled invitation and confirmed it renders "This invitation is no longer valid" — the exact generic message, not a leaked reason. `pnpm type-check && pnpm test (607/607) && pnpm security:boundaries` all clean; cleaned up the test organization afterward.
  - Follow-up (2026-07-22): the user asked, reasonably, how an invited person would ever find out — the honest answer was "only an email, and in this environment (no `RESEND_API_KEY`) that email silently never sends at all." Fixed both sides of that gap: (1) `listInvitationsForEmail` (organization-lifecycle.ts) + `GET /api/organizations/invitations/mine`, always the caller's own verified session email (never client-supplied, so no user can enumerate who else was invited where), backing a new `PendingInvitationsBanner` on the Dashboard matching the existing "First time here?" banner convention; (2) `sendOrganizationInvitationEmail`'s `devLink` (already returned in dev mode, per its own doc comment, but never read by anything) is now threaded through `inviteMember`/`resendInvitation`'s return values and the invite/resend routes, and `TeamSettingsPage` shows a "copy link" affordance next to any invitation whose email never actually sent. Verified live: invited the signed-in admin's own email into a third test organization (no second real account's credentials were available), confirmed the "Couldn't send the email — copy the link to share manually" note and copy button appeared, confirmed the copied link's target page rendered correctly, and confirmed the Dashboard banner showed "You've been invited to join Invite Visibility Test — as member" with a working "View invitation" link. `pnpm type-check && pnpm test (607/607) && pnpm security:boundaries` all clean; cleaned up the test organization afterward.

- [x] **Integrate active organization entitlement into billing**
  - Files: `src/routes/_dashboard/settings/billing.tsx`, `src/modules/dashboard/components/OrganizationBillingCard.tsx`, `src/modules/dashboard/components/OrganizationBillingCard.test.tsx`
  - Do: Replace user-plan Team assumptions with active organization entitlement and accepted-member plus usable-invitation seat usage. Members see provider organization/minimal state, admins see read-only billing, and only owners receive billing mutation capability. Export race-safe one-seat downgrade blockers and suspend non-owner Team access after entitlement loss without deleting membership/data. Switching organizations refreshes billing without mutating another organization.
  - Verify (2026-07-22): `pnpm vitest run src/modules/dashboard/components/OrganizationBillingCard.test.tsx` 8/8 passing — one test per role (owner/admin/member) plus free-tier CTA swap, lapsed/trial banner visibility per role, and a DTO-contamination test. Added `getOrganizationBillingDetail` and `OrganizationEntitlementDto`/`getOrganizationBillingSnapshot` to `organization-lifecycle.ts`/`contracts.ts` (same composition shape as `getTeamSnapshot`), behind a new `GET /api/organizations/billing` route that, like `team.ts`, only imports from `contracts.ts`. `OrganizationBillingCard` renders three tiers by role: member gets a one-line "X is on the Y plan" notice only; admin gets the full read-only card (tier, price, status, seat usage, features) with no plan-change control; owner gets everything admin sees plus the upgrade/compare-plans CTA and the "email us" copy — gated via `isOwnerRole`/`viewerRole`, never a hand-rolled string check. `billing.tsx` now uses `useActiveOrganizationId`/`organizationQueryKey` exactly like `team.tsx`, so switching organizations refetches billing scoped to the new org instead of leaving the previous org's card on screen — confirmed live: creating a second org and switching to it immediately showed its own Free-tier card (1/1 seats, reset usage), then switching back showed the first org's Team card again, no bleed-through either direction.
  - Downgrade blocker (2026-07-22): no self-serve downgrade mutation exists yet (no Stripe integration — `stripe-billing-platform` is still `pending` and depends on this plan), but a real gap existed in the one place a tier actually shrinks today: an admin plan grant (`setPlatformUserPlan`, called from `/api/admin/users/$userId` and `/api/admin/plan-requests`). A personal organization can carry more seats than `seat_limit` implies once upgraded to Team (task 3's own correction note), so nothing stopped an admin from shrinking it back to Pro/Free and silently stranding members over the new limit. Added `assertSeatLimitDowngradeIsSafe(organizationId, targetSeatLimit)` to `organization-lifecycle.ts` — same `select ... for update` row-locking pattern as `createInvitation`'s atomic seat check, throwing the same `SeatLimitExceededError` — and call it from `setPlatformUserPlan` before writing. Both admin routes now map that error to a clean 409 instead of an uncaught 500. Verified directly against the real dev database (bypassing HTTP, since the local admin account has no `ADMIN_USER_IDS` grant in this env): seeded a throwaway user whose personal org carried 2 real members on Team tier; `setPlatformUserPlan(user, 'pro', ...)` threw `SeatLimitExceededError` and left the `plans` row completely unchanged (no partial write), while `setPlatformUserPlan(user, 'team', ...)` (non-shrinking) still succeeded normally; `assertSeatLimitDowngradeIsSafe` alone was also exercised directly at the boundary (2 members vs. limits of 1/2/10) with the exact expected throw/pass split. Cleaned up all test rows afterward. No unit test was added for `assertSeatLimitDowngradeIsSafe`/`getOrganizationBillingDetail` themselves — consistent with this file's existing convention that real-authDb read helpers (`getSeatUsage`, `listMyOrganizations`, etc.) have none either, verified live instead of mocked, since mocking real Postgres row-locking wouldn't actually prove race-safety anyway.
  - "Team lapse" (2026-07-22): no cron/webhook expires entitlements in this codebase yet (nothing does — admin grants are the only mutation path), so "suspend non-owner Team access after entitlement loss without deleting membership/data" is implemented as reading the existing `paidActionsAllowed` flag (`resolveEntitlementPolicy`: false once status is `past_due`/`canceled` on a paid tier) rather than new expiry infrastructure. `OrganizationBillingCard` shows a lapsed-plan banner to every role, including members, since it affects what they can still do — membership/data are never touched by this component, only messaging. Verified via unit test fixtures (real DB reproduction of a lapsed org wasn't attempted — there's no product path to actually cancel a plan yet).
  - Descoped explicitly: the "Plan history" section (previously reading the legacy per-user `plan-changes` table) was removed rather than ported, since it's now definitively the wrong tenant and `organizationPlanChanges` has zero repository readers built anywhere — building one wasn't asked for by this task's file list and would blur into `stripe-billing-platform`'s scope.
  - `pnpm type-check && pnpm test (615/615) && pnpm security:boundaries && node scripts/check-route-coverage.mjs (75 routes) && pnpm lint` all clean on master after merge.

- [x] **Integrate ownership, account deletion, and organization deletion UX**
  - Files: `src/routes/_dashboard/settings/privacy.tsx`, `src/modules/dashboard/components/OrganizationDangerZone.tsx`, `src/modules/dashboard/components/OrganizationDangerZone.test.tsx`
  - Do: Surface foundation deletion guard when a subject is sole owner with other members; provide recent-auth transfer flow. Organization deletion uses owner-only challenge, grace period, cancel/status UI, and does not reuse account deletion. All operations display audit/reference IDs but no sensitive payload.
  - Real bug found and fixed (2026-07-22): `listOwnedOrganizations` (`account-privacy.ts`) counted every organization where the user is `owner`, and every account has its own solo personal workspace — so the account-deletion guard (`assertNoOwnedOrganizations`, `legal.ts`) was blocking **100% of deletion requests, unconditionally**, ever since a prior task fixed it to read live data instead of silently returning `[]`. Renamed to `listOwnedOrganizationsWithOtherMembers`: an inner join against a second reference to `organization_members` for "a different user in the same org" naturally scopes the guard to orgs that would actually orphan someone else's access, personal-flagged or not (an upgraded personal org can carry real teammates — task 3's own correction). `AccountDeletionOwnershipError` now carries `{organizationId, organizationName}[]` instead of bare ids. 5 new tests in `legal.test.ts` cover: solo personal org never blocks, an org with other members does, the error carries id+name, transfer away unblocks, and `performHardDelete`'s defense-in-depth re-check.
  - Organization deletion grace period (2026-07-22): new `organization_deletion_requests` table (migration `0023`, same `_auth_broker_all` RLS/grant pattern as `organizations`/`organization_members` — accessed via `authDb`, never `builderhunt_app`), deliberately not reusing `deletion_requests`/`legal.ts` since an organization's deletion affects every other member, not just the requester. `organization-lifecycle.ts` gained `requestOrganizationDeletion` (owner-only, `requireRecentAuthentication`, 30-day grace period, upserts on `organizationId` so re-requesting after a cancel reuses the same row) and `cancelOrganizationDeletion` (owner-only, no recent-auth needed — cancelling is the safe direction), replacing the old immediate-delete `deleteOrganization`. `DELETE /api/organizations` now schedules instead of deleting; new `DELETE /api/organizations/deletion` cancels. `processPendingOrganizationDeletions()` (standalone, no unit test — same live-verified-only precedent as `getSeatUsage`/`assertSeatLimitDowngradeIsSafe`, since mocking real Postgres row deletion wouldn't prove anything a mock doesn't already assume) hard-deletes every organization past its grace period and marks the request completed; wired into the existing `/api/admin/legal/run-worker` route alongside the account sweep.
  - `OrganizationDangerZone.tsx` (extracted from `TeamSettingsPage`, 10/10 tests passing): a typed-organization-name confirmation input gates the "Schedule deletion" button (the "owner-only challenge"); a pending-deletion banner with days-remaining + cancel button once a deletion is pending (hides the delete control entirely while pending); a dedicated "Sign in again to continue" CTA (linking to `/auth/sign-in?redirect=/settings/team`, reusing the same contract as the invitation-accept flow) for the recent-auth requirement's stale-session error specifically — that backend gate already existed and was tested (task 5), only the UI treated it as a generic error string before; and a `referenceId` prop (the audit event's own request id, or the deletion-request row's own id) shown after transfer/request-deletion/cancel. `transferOwnership` now returns `{requestId}` for exactly this.
  - Verified live end-to-end against the real dev database as the real signed-in owner: created a team org, seeded a second real member, confirmed account deletion was correctly **blocked** with "Deletion Verify Co" listed (and NOT the personal workspace) — transferred ownership via the privacy page's link, hit the real recent-auth wall (stale-session banner + working "sign in again" link to the right redirect target), refreshed the session's `authenticatedAt` and completed the transfer (reference id shown, role/owner swap confirmed in the members list), then confirmed account deletion was now unblocked (scheduled it, saw the reference id, then cancelled it — restoring the account to normal). Separately: scheduled deletion of a fresh test org (typed-name challenge confirmed gating the button), confirmed the `organization_deletion_requests` row and the pending banner, cancelled it (confirmed `cancelled` in DB), rescheduled it (confirmed the upsert reused the same row id), forced its grace period into the past, and ran `processPendingOrganizationDeletions()` directly against the real DB: `{"processed":1,"errors":0}` — the past-due org was hard-deleted while a second, not-yet-due test org was correctly left untouched (`pending`, still existed). Cleaned up all test organizations/members afterward and confirmed the personal workspace was unaffected throughout (tenant B — the not-due-yet org and the personal workspace — never touched by the sweep or by any of the above actions).
  - `pnpm type-check && pnpm test (634/634) && pnpm security:boundaries && node scripts/check-route-coverage.mjs (76 routes) && pnpm lint && node scripts/db/verify-migration-integrity.mjs` all clean on master after merge.

- [ ] **Run the Team isolation and release matrix**
  - Files: `e2e/team-accounts.spec.ts`, `test/security/team-cache-isolation.test.ts`, `docs/operations/team-accounts.md`, `.github/workflows/quality.yml`
  - Do: Seed users with personal plus A/B memberships and exercise switch, create, invite, accept, role, removal, transfer, billing, export/deletion links, stale tabs, and final-seat race. Run using non-owner app DB role with RLS; include accessibility and mobile/keyboard checks.
  - Verify: `pnpm test:security && pnpm test:rls && pnpm test:e2e -- e2e/team-accounts.spec.ts && pnpm lint && pnpm type-check && pnpm test && pnpm build` passes before unblocking `shared-resources`.

## Future

- Custom roles, nested teams, SSO/SAML, SCIM, domain policies, and per-seat payment automation.
